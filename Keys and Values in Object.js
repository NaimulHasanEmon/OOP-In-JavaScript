let person = {
    name: "Emon",
    age: 23,
    profession: 'Developer',
    skills: ['HTML', 'CSS', 'JavaScript'],
    salary: 30000,
    isMarried: true,
}

// To see the keys of the object
let keys = Object.keys(person);
console.log(keys);

// To see the values of the object
let values = Object.values(person);
console.log(values);